# Configuração do Frontend (Vite)

# URL da API
VITE_API_URL=http://localhost:8787/api/v1

# Chave Pública VAPID para Web Push (Notificações)
# IMPORTANTE: Apenas a chave PÚBLICA (nunca a privada)
VITE_VAPID_PUBLIC_KEY=sua-chave-publica
